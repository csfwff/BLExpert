# BLExpert Agent 协作说明

> 当前基线（2026-08-19）：BLE 基础通信、工作区、标准协议封包/解帧、设备默认连接配置、导入脚本信任边界和首轮 feature 组件拆分已实现。后续开发规范见 [Flutter 开发规范](docs/Flutter开发规范.md)，优先级见 [开发路线图](docs/开发路线图.md)。

## 角色定位

本仓库是 BLExpert 的 Flutter 工程。BLExpert 是一款跨平台蓝牙调试与分析工具，目标平台包括 Android、iOS、Windows、macOS 和 Web。

在本项目中工作的 Agent 应优先做到：

- 优先使用中文沟通、中文文档和中文界面文案。
- 优先保证跨平台兼容性。
- 变更范围保持清晰、克制，避免无关重构。
- 代码结构要方便后续扩展 BLE、经典蓝牙、工作区管理和脚本引擎。
- 对协议处理、数据解析、平台差异等不直观逻辑添加必要注释。

## 产品背景

BLExpert 面向物联网和嵌入式开发场景，用于解决设备协议差异大、调试过程分散、团队协作难的问题。

应用后续应具备：

- 工作区化的设备配置。
- BLE 和经典蓝牙通信。
- 围绕原始 HEX 数据的 JavaScript 脚本处理。
- 结构化数据解析和字段映射。
- 完整工作区导入 / 导出。
- 实时数据日志与可视化。

## 实现优先级

新增功能时，优先按以下顺序推进：

1. 公共模型与数据契约。
2. 工作区持久化。
3. 蓝牙抽象层与平台适配层。
4. 扫描、连接、断开流程。
5. 数据日志与 HEX / ASCII / JSON 格式化工具。
6. 脚本引擎集成。
7. 数据解析、映射、导入导出。

## 架构约定

- 平台特定能力必须放在服务层或适配层，UI 不应直接依赖具体蓝牙插件。
- 接入真实插件前，优先保持 Mock 服务可用，方便桌面和 Web 预览。
- 蓝牙能力要优雅处理不支持的平台，尤其是 Web 和桌面平台。
- 数据模型要天然支持 JSON 序列化。
- 工作区导出格式应稳定、可读、便于版本升级。

## UI 约定

- 界面文案优先使用中文，界面要支持国际化。
- 支持亮色、暗色和跟随系统三种主题模式。
- 默认主题模式为跟随系统。
- 首页直接呈现工具能力，不做营销页。
- 设备列表、数据日志、字段映射等界面应紧凑、专业、便于扫描。
- 所有移动端和桌面端布局都要避免文字溢出和控件重叠。
- 尽量少用弹窗。

## 编码约定

- 目录职责、依赖方向、组件粒度、`part` 使用条件和提交门槛统一遵循 [Flutter 开发规范](docs/Flutter开发规范.md)。
- `main.dart` 仅负责应用启动；禁止重新放入页面、服务或业务状态。
- 新增页面组件按 feature 归档，普通 `import` 优先；仅紧密共享私有类型的同一 feature library 可以使用 `part`。
- 代码标识符沿用 Dart / Flutter 常规英文命名。
- 注释优先使用中文，除非引用第三方 API 或协议原文。
- 默认使用 ASCII；中文文档、中文界面文案、中文注释可以使用 UTF-8。
- 仅在真正降低复杂度或贴合现有模式时新增抽象。
- 依赖保持克制，等功能需要明确后再引入。
- 不要考虑旧数据兼容，我们还没发布版本

## 当前开发重点

优先完成：

- 脚本执行的可终止超时、发送速率/危险命令策略和原生 runtime 能力审计。
- 工作区版本迁移、导入预览与冲突处理。
- 会话记录的持久化、筛选、书签和导出。
- 控制台、发送编辑器、配置草稿与可访问性的第二轮改造。
- Android、iOS、Windows、macOS、Linux、Web 的真实设备回归。

标准协议收发、连接默认配置和导入脚本信任边界已经具备基础实现；不要将它们重新列为待开发项。
