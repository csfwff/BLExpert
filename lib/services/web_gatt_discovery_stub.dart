import 'web_gatt_types.dart';

Future<List<WebGattCharacteristicInfo>?> discoverWebGattCharacteristics(
  String deviceId,
) async => null;

Future<bool> isWebGattConnected(String deviceId) async => false;
